# Supabase Configuration (Cloud Database - Optional but Recommended)
# Get these from https://supabase.com after creating a new project
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# If you don't set these, the app will use local SQLite database
# To use Supabase (recommended for production):
# 1. Go to https://supabase.com
# 2. Create a new project (free)
# 3. Get your URL and anon key from Settings > API
# 4. Replace the values above
# 5. The app will automatically switch to Supabase

# Other configurations
JWT_SECRET=your-jwt-secret-here
NODE_ENV=development
PORT=8080

# Note: Supabase is free and provides:
# - 500MB database storage
# - Up to 50,000 monthly active users
# - Real-time subscriptions
# - Row Level Security
# - Automatic backups
# - Dashboard for managing data
